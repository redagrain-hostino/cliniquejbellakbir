---

---

<section class="emergency-banner" aria-labelledby="emergency-title">
    <div class="emergency-container">
        <div class="emergency-content">
            <h2 id="emergency-title" class="emergency-title">
                Disponible 7j/7 et 24h/24,<br />toujours à vos côtés
            </h2>
            <p class="emergency-desc">
                À la Clinique Jbel Lakbir de Tanger, notre service d’urgence est
                ouvert 24h/24 et 7j/7 pour vous accueillir, vous écouter et
                prendre soin de vous avec bienveillance.
            </p>
        </div>
        <div class="emergency-action">
            <a href="/contact" class="emergency-cta">Prendre rendez-vous</a>
        </div>
    </div>
</section>

<style>
    :root {
        --emergency-bg: #33a9b1; /* Teal background */
        --emergency-text-dark: #ffffff;
        --emergency-font-heading: "Raleway", sans-serif;
        --emergency-font-body: "Poppins", sans-serif;
    }

    .emergency-banner {
        background-color: var(--emergency-bg);
        padding: 80px 20px;
    }

    .emergency-container {
        max-width: 1400px;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        gap: 35px;
    }

    .emergency-content {
        flex: 1;
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .emergency-title {
        font-family: var(--emergency-font-heading);
        font-size: 48px;
        font-weight: 700;
        color: var(--emergency-text-dark);
        margin: 0 0 20px;
        line-height: 1.25;
    }

    .emergency-desc {
        font-family: var(--emergency-font-body);
        font-size: 16px;
        color: var(--emergency-text-dark);
        opacity: 0.9; /* Slight increase in opacity for better readability on teal */
        margin: 0;
        line-height: 1.6;
        max-width: 800px;
    }

    .emergency-action {
        flex-shrink: 0;
    }

    .emergency-cta {
        display: inline-block;
        background-color: #ffffff; /* White button */
        color: #33a9b1; /* Teal text */
        border: 2px solid #ffffff;
        font-family: var(--emergency-font-body);
        font-size: 16px;
        font-weight: 600;
        padding: 13px 33px; /* Adjusted to account for border */
        border-radius: 5px;
        text-decoration: none;
        transition:
            background-color 0.3s ease,
            color 0.3s ease,
            transform 0.2s ease;
    }

    .emergency-cta:hover {
        background-color: transparent;
        color: #ffffff;
        transform: translateY(-2px);
    }

    /* Responsive Adjustments */
    @media (max-width: 767px) {
        .emergency-container {
            flex-direction: column;
            text-align: center;
        }

        .emergency-title {
            font-size: 28px;
        }
    }
</style>
